import { ExhibitorFilterPipe } from "./exhibitor-filter.pipe";

describe('ExhibitorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExhibitorFilterPipe();
    expect(pipe).toBeTruthy();
  });
}); 
